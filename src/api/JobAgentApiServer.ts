import { createServer, IncomingMessage, ServerResponse, Server } from "node:http";
import { AddressInfo } from "node:net";
import { Database } from "../database/Database";
import { JOB_PLATFORM_REGISTRY } from "../jobs/sources/JobPlatformRegistry";

interface SummaryRow { jobs:string; matches:string; applications:string; recruiters:string; contacts:string; content:string; outreachSent:string; pendingTasks:string; matchApply:string; matchReview:string; matchReject:string; verifiedRecruiterEmails:string; currentRecruiters:string; recentRecruiters:string; topMatches:unknown[]; recruiterLeads:unknown[]; }
export interface JobAgentApiServerOptions { host?:string; port?:number }

export class JobAgentApiServer {
  private server:Server|null=null;
  constructor(private readonly database:Database,private readonly options:JobAgentApiServerOptions={}){}
  async start():Promise<void>{
    if(this.server)return; const host=this.options.host??process.env.API_HOST??"127.0.0.1"; const port=this.options.port??Number(process.env.API_PORT??3000);
    if(!Number.isInteger(port)||port<0||port>65535)throw new Error("API_PORT must be a valid TCP port.");
    const server=createServer((request,response)=>{void this.handle(request,response).catch((error:unknown)=>{writeJson(response,500,{status:"ERROR",error:"Internal server error"});console.error("Job Agent API request failed:",error instanceof Error?error.message:String(error));});});
    await new Promise<void>((resolve,reject)=>{const onError=(error:Error):void=>{server.off("listening",onListening);reject(error)};const onListening=():void=>{server.off("error",onError);resolve()};server.once("error",onError);server.once("listening",onListening);server.listen(port,host)});this.server=server;
  }
  getAddress():{host:string;port:number}|null{const address=this.server?.address();if(!address||typeof address==="string")return null;const info=address as AddressInfo;return{host:info.address,port:info.port}}
  async stop():Promise<void>{const server=this.server;this.server=null;if(!server)return;await new Promise<void>(resolve=>server.close(()=>resolve()))}
  private async handle(request:IncomingMessage,response:ServerResponse):Promise<void>{
    const method=request.method??"GET";const url=new URL(request.url??"/","http://job-agent.local");response.setHeader("cache-control","no-store");
    if(method!=="GET"){response.setHeader("allow","GET");writeJson(response,405,{status:"METHOD_NOT_ALLOWED"});return}
    if(url.pathname==="/healthz"){try{await this.database.query("SELECT 1");writeJson(response,200,{status:"ok",database:"ok"})}catch{writeJson(response,503,{status:"degraded",database:"unavailable"})}return}
    if(url.pathname==="/api/jobs"){
      const limit=parseLimit(url.searchParams.get("limit"));const decision=url.searchParams.get("decision");const search=(url.searchParams.get("search")??"").trim();const params:unknown[]=[];const where:string[]=[];
      if(decision&&["APPLY","REVIEW","REJECT"].includes(decision)){params.push(decision);where.push("m.decision = $"+params.length)}
      if(search){params.push("%"+search+"%");where.push("(j.company_name ILIKE $"+params.length+" OR j.title ILIKE $"+params.length+" OR COALESCE(j.location,'') ILIKE $"+params.length+" OR COALESCE(o.platform,'') ILIKE $"+params.length+")")}
      params.push(limit);const jobs=await this.database.query(`SELECT j.id,j.company_name AS company,j.title AS role,j.location,j.workplace_type AS "workplaceType",j.canonical_url AS url,j.posted_at,m.decision,m.match_score AS score,m.reason,COALESCE(o.platform,'Unknown') AS platform,o.source_url AS "sourceUrl" FROM job_opportunities j LEFT JOIN LATERAL (SELECT decision,match_score,reason FROM match_decisions WHERE job_opportunity_id=j.id ORDER BY created_at DESC LIMIT 1) m ON true LEFT JOIN LATERAL (SELECT platform,source_url FROM job_observations WHERE job_opportunity_id=j.id ORDER BY observed_at DESC LIMIT 1) o ON true ${where.length?"WHERE "+where.join(" AND "):""} ORDER BY j.posted_at DESC NULLS LAST,j.created_at DESC LIMIT $${params.length}`,params);writeJson(response,200,{jobs:jobs.rows});return;
    }
    if(url.pathname==="/api/applications"){
      const limit=parseLimit(url.searchParams.get("limit"));const applications=await this.database.query(`SELECT a.id,COALESCE((SELECT jo.company_name FROM job_opportunities jo WHERE jo.id=a.job_opportunity_id),(SELECT jo.company_name FROM jobs jb JOIN job_opportunities jo ON jo.id=jb.job_opportunity_id WHERE jb.id=a.job_id)) AS company,COALESCE((SELECT jo.title FROM job_opportunities jo WHERE jo.id=a.job_opportunity_id),(SELECT jo.title FROM jobs jb JOIN job_opportunities jo ON jo.id=jb.job_opportunity_id WHERE jb.id=a.job_id)) AS role,COALESCE((SELECT jo.location FROM job_opportunities jo WHERE jo.id=a.job_opportunity_id),(SELECT jo.location FROM jobs jb JOIN job_opportunities jo ON jo.id=jb.job_opportunity_id WHERE jb.id=a.job_id)) AS location,COALESCE((SELECT jo.canonical_url FROM job_opportunities jo WHERE jo.id=a.job_opportunity_id),(SELECT jo.canonical_url FROM jobs jb JOIN job_opportunities jo ON jo.id=jb.job_opportunity_id WHERE jb.id=a.job_id)) AS url,(SELECT m.match_score FROM match_decisions m WHERE m.job_opportunity_id=a.job_opportunity_id ORDER BY m.created_at DESC LIMIT 1) AS score,(SELECT m.reason FROM match_decisions m WHERE m.job_opportunity_id=a.job_opportunity_id ORDER BY m.created_at DESC LIMIT 1) AS reason,a.status,a.created_at FROM applications a ORDER BY a.created_at DESC LIMIT $1`,[limit]);writeJson(response,200,{applications:applications.rows});return;
    }
    if(url.pathname==="/api/source-health"){
      const sources=await this.database.query(`SELECT s.id,s.name,s.source_type AS "sourceType",s.status,s.last_run_at AS "lastRunAt",s.last_success_at AS "lastSuccessAt",s.consecutive_failures AS "consecutiveFailures",s.disabled_until AS "disabledUntil",COALESCE(r.fetched_count,0) AS "lastFetched",COALESCE(r.inserted_count,0) AS "lastInserted",COALESCE(r.duplicate_count,0) AS "lastDuplicates",r.status AS "lastRunStatus",r.finished_at AS "lastFinishedAt",r.error_summary AS "lastError" FROM sources s LEFT JOIN LATERAL (SELECT status,fetched_count,inserted_count,duplicate_count,finished_at,error_summary FROM source_runs WHERE source_id=s.id ORDER BY started_at DESC LIMIT 1) r ON true ORDER BY s.consecutive_failures DESC,s.last_success_at ASC NULLS FIRST,s.name ASC`);writeJson(response,200,{sources:sources.rows});return;
    }
    if(url.pathname==="/api/platform-coverage"){
      const latest=await this.database.query<{platform_id:string;platform_name:string;capability:string;outcome:string;extraction_mode:string|null;fetched:string;normalized:string|null;inserted:string;duplicates:string;duration_ms:string;completed_at:string;error_detail:string|null}>(`SELECT DISTINCT ON (platform_id) platform_id,platform_name,capability,outcome,extraction_mode,fetched,COALESCE(normalized,fetched) AS normalized,inserted,duplicates,duration_ms,completed_at,error_detail FROM platform_discovery_runs ORDER BY platform_id,completed_at DESC`);
      const counts=new Map<string,number>();for(const platform of JOB_PLATFORM_REGISTRY)counts.set(platform.capability,(counts.get(platform.capability)??0)+1);const rows=latest.rows;const countOutcome=(v:string)=>rows.filter(r=>r.outcome===v).length;const executable=JOB_PLATFORM_REGISTRY.filter(p=>p.capability!=="catalog-only").length;const registryIds=new Set(JOB_PLATFORM_REGISTRY.map(p=>p.id));const attempted=rows.filter(r=>registryIds.has(r.platform_id)&&r.capability!=="catalog-only").length;const catalogRows=JOB_PLATFORM_REGISTRY.filter(p=>p.capability==="catalog-only").map(p=>({platform_id:p.id,platform_name:p.name,capability:p.capability,outcome:"SKIPPED_CATALOG_ONLY",extraction_mode:null,fetched:"0",normalized:"0",inserted:"0",duplicates:"0",duration_ms:"0",completed_at:null,error_detail:"Catalog entry has no executable direct adapter."}));
      writeJson(response,200,{registry:{total:JOB_PLATFORM_REGISTRY.length,activeAdapters:counts.get("active-adapter")??0,configurableAdapters:counts.get("configurable-adapter")??0,catalogOnly:counts.get("catalog-only")??0,executable},latestRuntime:{attempted,succeeded:countOutcome("SUCCESS_WITH_JOBS")+countOutcome("SUCCESS_ZERO_JOBS"),empty:countOutcome("SUCCESS_ZERO_JOBS"),failed:rows.filter(r=>!["SUCCESS_WITH_JOBS","SUCCESS_ZERO_JOBS","TIMEOUT","RATE_LIMITED","BLOCKED_OR_RESTRICTED"].includes(r.outcome)).length,timeout:countOutcome("TIMEOUT"),rateLimited:countOutcome("RATE_LIMITED"),blocked:countOutcome("BLOCKED_OR_RESTRICTED"),parserErrors:countOutcome("PARSER_ERROR"),fetched:rows.reduce((s,r)=>s+Number(r.fetched),0),normalized:rows.reduce((s,r)=>s+Number(r.normalized??r.fetched),0),inserted:rows.reduce((s,r)=>s+Number(r.inserted),0),duplicates:rows.reduce((s,r)=>s+Number(r.duplicates),0)},platforms:[...rows,...catalogRows]});return;
    }
    if(url.pathname==="/api/recruiters"){
      const limit=parseLimit(url.searchParams.get("limit"));const recruiters=await this.database.query(`SELECT c.id,c.full_name AS name,c.company_name AS company,c.title AS role,c.email,c.email_status AS "emailStatus",c.verified,c.mailbox_evidence AS "mailboxEvidence",c.relevance_status AS relevance,c.relevance_score AS "relevanceScore",c.confidence,(SELECT s.source_url FROM recruiter_contact_sources s WHERE s.recruiter_contact_id=c.id ORDER BY s.observed_at DESC LIMIT 1) AS "profileUrl",(SELECT s.source_type FROM recruiter_contact_sources s WHERE s.recruiter_contact_id=c.id ORDER BY s.observed_at DESC LIMIT 1) AS "evidenceType" FROM recruiter_contacts c WHERE c.relevance_status IN ('CURRENT','RECENT') ORDER BY c.relevance_score DESC NULLS LAST,c.confidence DESC NULLS LAST,c.updated_at DESC LIMIT $1`,[limit]);writeJson(response,200,{recruiters:recruiters.rows});return;
    }
    if(url.pathname==="/api/contacts"){
      const limit=parseLimit(url.searchParams.get("limit"));const contacts=await this.database.query(`SELECT c.id,c.normalized_email AS email,c.domain,c.validation_status AS "validationStatus",c.relevance_score AS "relevanceScore",c.evidence_context AS "evidenceContext",c.observed_at AS "observedAt",r.source_url AS "sourceUrl",r.source_type AS "sourceType",r.title AS "resourceTitle",r.status AS "resourceStatus" FROM public_contact_resource_contacts c JOIN public_contact_resources r ON r.id=c.resource_id WHERE c.validation_status <> 'INVALID' AND c.relevance_score > 0 ORDER BY c.relevance_score DESC,c.updated_at DESC LIMIT $1`,[limit]);writeJson(response,200,{contacts:contacts.rows});return;
    }
    if(url.pathname==="/api/content"){
      const limit=parseLimit(url.searchParams.get("limit"));const content=await this.database.query(`SELECT s.id,c.full_name AS recruiter,c.company_name AS company,c.title AS role,s.source_url AS "sourceUrl",s.source_type AS "sourceType",s.confidence,s.observed_at AS "observedAt",c.relevance_evidence AS evidence FROM recruiter_contact_sources s JOIN recruiter_contacts c ON c.id=s.recruiter_contact_id WHERE LOWER(COALESCE(s.source_type,'')) IN ('job_posting','current_job_posting','current_role','recent_job_posting','recent_role','job_hiring_evidence') ORDER BY s.observed_at DESC LIMIT $1`,[limit]);writeJson(response,200,{content:content.rows});return;
    }
    if(url.pathname==="/api/matches"){
      const limit=parseLimit(url.searchParams.get("limit"));const decision=(url.searchParams.get("decision")??"").trim().toUpperCase();const params:unknown[]=[];let where="";if(["APPLY","REVIEW","REJECT"].includes(decision)){params.push(decision);where="WHERE latest.decision=$1"}params.push(limit);const matches=await this.database.query(`WITH latest AS (SELECT DISTINCT ON (job_opportunity_id) job_opportunity_id,decision,match_score,reason,created_at FROM match_decisions ORDER BY job_opportunity_id,created_at DESC) SELECT latest.job_opportunity_id AS "jobId",j.company_name AS company,j.title AS role,j.location,j.canonical_url AS url,latest.decision,latest.match_score AS score,latest.reason,latest.created_at AS "decidedAt",COALESCE(obs.platform,'Unknown') AS platform,obs.source_url AS "sourceUrl" FROM latest JOIN job_opportunities j ON j.id=latest.job_opportunity_id LEFT JOIN LATERAL (SELECT platform,source_url FROM job_observations WHERE job_opportunity_id=j.id ORDER BY observed_at DESC LIMIT 1) obs ON true ${where} ORDER BY latest.match_score DESC NULLS LAST,j.posted_at DESC NULLS LAST LIMIT $${params.length}`,params);writeJson(response,200,{matches:matches.rows});return;
    }
    if(url.pathname==="/api/summary"){
      const summary=await this.database.query<SummaryRow>(`WITH latest_matches AS (SELECT DISTINCT ON (job_opportunity_id) job_opportunity_id,decision,match_score,reason,created_at FROM match_decisions ORDER BY job_opportunity_id,created_at DESC) SELECT (SELECT COUNT(*)::text FROM job_opportunities) AS jobs,(SELECT COUNT(*)::text FROM latest_matches) AS matches,(SELECT COUNT(*)::text FROM applications) AS applications,(SELECT COUNT(*)::text FROM recruiter_contacts) AS recruiters,(SELECT COUNT(*)::text FROM public_contact_resource_contacts WHERE validation_status <> 'INVALID' AND relevance_score > 0) AS contacts,(SELECT COUNT(*)::text FROM recruiter_contact_sources WHERE LOWER(COALESCE(source_type,'')) IN ('job_posting','current_job_posting','current_role','recent_job_posting','recent_role','job_hiring_evidence')) AS content,(SELECT COUNT(*)::text FROM recruiter_outreach_messages WHERE status='SENT') AS "outreachSent",(SELECT COUNT(*)::text FROM tasks WHERE status IN ('PENDING','RUNNING')) AS "pendingTasks",(SELECT COUNT(*)::text FROM latest_matches WHERE decision='APPLY') AS "matchApply",(SELECT COUNT(*)::text FROM latest_matches WHERE decision='REVIEW') AS "matchReview",(SELECT COUNT(*)::text FROM latest_matches WHERE decision='REJECT') AS "matchReject",(SELECT COUNT(*)::text FROM recruiter_contacts WHERE verified=true AND email_status='VERIFIED' AND mailbox_evidence=true) AS "verifiedRecruiterEmails",(SELECT COUNT(*)::text FROM recruiter_contacts WHERE relevance_status='CURRENT') AS "currentRecruiters",(SELECT COUNT(*)::text FROM recruiter_contacts WHERE relevance_status='RECENT') AS "recentRecruiters",(SELECT COALESCE(jsonb_agg(x ORDER BY x.score DESC,x.posted_at DESC NULLS LAST),'[]'::jsonb) FROM (SELECT j.company_name AS company,j.title AS role,j.location,j.canonical_url AS url,m.decision,m.match_score AS score,m.reason,j.posted_at FROM latest_matches m JOIN job_opportunities j ON j.id=m.job_opportunity_id ORDER BY m.match_score DESC,j.posted_at DESC NULLS LAST LIMIT 10) x) AS "topMatches",(SELECT COALESCE(jsonb_agg(x ORDER BY x."relevanceScore" DESC NULLS LAST,x.confidence DESC NULLS LAST,x.updated_at DESC),'[]'::jsonb) FROM (SELECT c.full_name AS name,c.company_name AS company,c.title AS role,c.relevance_status AS relevance,c.relevance_score AS "relevanceScore",c.relevance_evidence AS "hiringEvidence",(SELECT s.source_url FROM recruiter_contact_sources s WHERE s.recruiter_contact_id=c.id ORDER BY s.observed_at DESC LIMIT 1) AS "profileUrl",c.email,c.email_status AS "emailStatus",c.verified,c.mailbox_evidence AS "mailboxEvidence",c.confidence,(c.verified=true AND c.email_status='VERIFIED' AND c.mailbox_evidence=true AND c.relevance_status IN ('CURRENT','RECENT') AND COALESCE(c.suppressed,false)=false AND COALESCE(c.confidence,0)>=80) AS "eligibleForOutreach",c.updated_at FROM recruiter_contacts c WHERE c.relevance_status IN ('CURRENT','RECENT') ORDER BY c.relevance_score DESC NULLS LAST,c.confidence DESC NULLS LAST,c.updated_at DESC LIMIT 10) x) AS "recruiterLeads"`);writeJson(response,200,summary.rows[0]??{});return;
    }
    if(url.pathname==="/"){response.writeHead(200,{"content-type":"text/html; charset=utf-8"});response.end(DASHBOARD_HTML);return}writeJson(response,404,{status:"NOT_FOUND"});
  }
}
function parseLimit(value:string|null):number{const parsed=Number(value??25);if(!Number.isInteger(parsed)||parsed<1)return 25;return Math.min(parsed,100)}
function writeJson(response:ServerResponse,statusCode:number,value:unknown):void{response.writeHead(statusCode,{"content-type":"application/json; charset=utf-8"});response.end(JSON.stringify(value))}

const DASHBOARD_HTML=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#0b1220"><title>Job Agent — Job Intelligence</title><style>:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#172033;background:#f5f7fb}*{box-sizing:border-box}body{margin:0;background:linear-gradient(180deg,#f8fafc,#f3f6fb);min-height:100vh}button,input{font:inherit}button{cursor:pointer}.app{max-width:1480px;margin:auto;padding:22px}.topbar{display:flex;align-items:center;justify-content:space-between;gap:18px;padding:18px 20px;background:#0b1220;color:#fff;border-radius:18px;box-shadow:0 14px 40px rgba(15,23,42,.16)}.brand{display:flex;align-items:center;gap:13px}.logo{width:42px;height:42px;border-radius:12px;background:linear-gradient(135deg,#7c3aed,#2563eb);display:grid;place-items:center;font-weight:800}.brand h1{font-size:18px;margin:0}.brand p{margin:3px 0 0;color:#aab6ca;font-size:12px}.top-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap;justify-content:flex-end}.signal{display:inline-flex;align-items:center;gap:7px;padding:7px 10px;border-radius:999px;background:rgba(255,255,255,.08);font-size:12px;color:#d9e1ee}.dot{width:7px;height:7px;border-radius:50%;background:#34d399}.tabs{display:flex;gap:6px;margin:18px 0;overflow:auto;padding-bottom:2px}.tab,.filter{border:1px solid #d8dee9;background:#fff;color:#536176;border-radius:10px;padding:9px 12px;white-space:nowrap}.tab.active,.filter.active{background:#111a2d;color:#fff;border-color:#111a2d}.hero{display:grid;grid-template-columns:1.4fr .6fr;gap:16px;margin-bottom:16px}.hero-card,.panel,.kpi{background:#fff;border:1px solid #e2e8f0;border-radius:16px;box-shadow:0 7px 24px rgba(15,23,42,.05)}.hero-card{padding:24px}.eyebrow{font-size:11px;text-transform:uppercase;letter-spacing:.12em;font-weight:800;color:#6b7280}.hero h2{margin:7px 0 8px;font-size:28px;letter-spacing:-.03em}.hero p{margin:0;color:#667085;line-height:1.55}.hero-meta{display:flex;gap:8px;flex-wrap:wrap;margin-top:17px}.badge{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:5px 9px;background:#f1f5f9;color:#475569;font-size:11px;font-weight:700}.badge.safe{background:#ecfdf3;color:#067647}.badge.warn{background:#fff7ed;color:#c2410c}.runtime{padding:20px}.runtime-row{display:flex;justify-content:space-between;gap:12px;padding:9px 0;border-bottom:1px solid #edf1f5;font-size:13px}.runtime-row:last-child{border-bottom:0}.runtime-row span:first-child{color:#667085}.kpis{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:12px;margin-bottom:16px}.kpi{padding:17px}.kpi .label{color:#667085;font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.06em}.kpi .num{font-size:27px;font-weight:800;margin-top:7px}.kpi .sub{font-size:11px;color:#98a2b3;margin-top:4px}.layout{display:grid;grid-template-columns:minmax(0,1.5fr) minmax(340px,.7fr);gap:16px}.panel{padding:19px;margin-bottom:16px}.panel-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:15px}.panel h3{margin:0;font-size:16px}.panel-note{font-size:11px;color:#98a2b3;margin-top:4px}.toolbar{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:13px}.toolbar input{flex:1;min-width:190px;border:1px solid #d6dce6;border-radius:9px;padding:9px 11px;outline:none}.list{display:grid;gap:9px}.job{border:1px solid #e7ebf1;border-radius:12px;padding:14px;transition:.12s}.job:hover{border-color:#c9d2e0;box-shadow:0 7px 18px rgba(15,23,42,.06);transform:translateY(-1px)}.job-title{font-weight:750;font-size:14px}.job-company{color:#475467;font-size:12px;margin-top:3px}.job-meta{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px}.decision{font-size:10px;font-weight:800;padding:4px 7px;border-radius:999px}.apply{background:#ecfdf3;color:#067647}.review{background:#fff7ed;color:#b54708}.reject{background:#f2f4f7;color:#475467}.platform{background:#eef2ff;color:#4338ca}.score{background:#f0fdf4;color:#15803d}.job-reason{font-size:12px;color:#667085;line-height:1.45;margin-top:9px}.row-link{display:inline-block;margin-top:9px;color:#4f46e5;font-size:11px;text-decoration:none}.empty{padding:25px 10px;text-align:center;color:#667085;font-size:12px;border:1px dashed #d6dce6;border-radius:11px}.mini-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.mini{padding:12px;border:1px solid #e7ebf1;border-radius:11px}.mini span{display:block;color:#667085;font-size:10px;text-transform:uppercase;letter-spacing:.05em}.mini strong{display:block;margin-top:4px;font-size:18px}.progress{height:7px;background:#edf1f5;border-radius:99px;overflow:hidden;margin-top:10px}.progress i{display:block;height:100%;background:linear-gradient(90deg,#7c3aed,#2563eb);border-radius:inherit}.table-wrap{overflow:auto;border:1px solid #e7ebf1;border-radius:11px}.table{width:100%;border-collapse:collapse;min-width:760px;font-size:12px}.table th{text-align:left;background:#f8fafc;color:#667085;font-size:10px;text-transform:uppercase;letter-spacing:.05em;padding:10px;border-bottom:1px solid #e7ebf1;white-space:nowrap}.table td{padding:10px;border-bottom:1px solid #eef1f5;vertical-align:top}.table tr:last-child td{border-bottom:0}.detail{position:fixed;inset:0;background:rgba(15,23,42,.42);display:none;justify-content:flex-end;z-index:10}.detail.open{display:flex}.drawer{height:100%;width:min(560px,100%);background:#fff;padding:22px;overflow:auto;box-shadow:-15px 0 45px rgba(15,23,42,.2)}.drawer-head{display:flex;justify-content:space-between;gap:12px;align-items:center}.close{border:0;background:#eef2f6;border-radius:9px;width:34px;height:34px}.detail-section{padding:15px 0;border-bottom:1px solid #edf1f5}.detail-section h4{margin:0 0 8px;font-size:11px;text-transform:uppercase;color:#667085;letter-spacing:.07em}.hidden{display:none}.ok{color:#067647}.bad{color:#b42318}.footer-note{font-size:11px;color:#98a2b3;text-align:center;padding:12px}@media(max-width:1100px){.kpis{grid-template-columns:repeat(3,1fr)}.hero{grid-template-columns:1fr}.layout{grid-template-columns:1fr}}@media(max-width:650px){.app{padding:11px}.topbar{border-radius:14px;padding:14px}.top-actions{justify-content:flex-start}.kpis{grid-template-columns:repeat(2,1fr)}.hero-card{padding:18px}.hero h2{font-size:23px}.panel{padding:14px}.brand p{display:none}}</style></head><body><div class="app"><header class="topbar"><div class="brand"><div class="logo">JA</div><div><h1>Job Agent</h1><p>Job intelligence, matching and evidence</p></div></div><div class="top-actions"><span class="signal"><i class="dot"></i><span id="online">System online</span></span><span class="signal">Automation OFF</span><span class="signal">Dry run ON</span><span class="signal">Gmail OFF</span><span class="signal">Outreach OFF</span></div></header><nav class="tabs" id="tabs"><button class="tab active" data-view="overview">Overview</button><button class="tab" data-view="jobs">Jobs</button><button class="tab" data-view="applications">Applications</button><button class="tab" data-view="recruiters">Recruiters</button><button class="tab" data-view="contacts">Contacts</button><button class="tab" data-view="content">Content</button><button class="tab" data-view="platforms">Platform coverage</button><button class="tab" data-view="sources">Source health</button></nav><section class="hero"><div class="hero-card"><div class="eyebrow">Live local workspace</div><h2>Turn public hiring signals into a focused job queue.</h2><p>Everything below is read from the local API and database. No mock records are used, and safety gates stay visible at all times.</p><div class="hero-meta"><span class="badge safe">Live API data</span><span class="badge safe">Applications dry-run</span><span class="badge">Platform provenance</span><span class="badge">Evidence-first leads</span></div></div><div class="hero-card runtime"><div class="eyebrow">Runtime</div><div class="runtime-row"><span>API</span><strong id="apiStatus">Checking…</strong></div><div class="runtime-row"><span>Database</span><strong id="dbStatus">Checking…</strong></div><div class="runtime-row"><span>Last refresh</span><strong id="lastRefresh">—</strong></div><div class="runtime-row"><span>Pending tasks</span><strong id="pending">—</strong></div></div></section><div class="kpis" id="kpis"></div><div id="view-overview"><div class="layout"><div><section class="panel"><div class="panel-head"><div><h3>Top opportunities</h3><div class="panel-note">Latest high-scoring matches with source provenance</div></div><button class="filter" data-go="jobs">Open explorer</button></div><div id="topMatches" class="list"></div></section><section class="panel"><div class="panel-head"><div><h3>Application queue</h3><div class="panel-note">Prepared records are not submitted</div></div></div><div id="overviewApps" class="list"></div></section></div><div><section class="panel"><div class="panel-head"><div><h3>Platform coverage</h3><div class="panel-note">Registry vs actual runtime execution</div></div><button class="filter" data-go="platforms">Explore</button></div><div class="mini-grid" id="platformMini"></div><div class="progress"><i id="platformProgress" style="width:0%"></i></div></section><section class="panel"><div class="panel-head"><div><h3>Recruiter intelligence</h3><div class="panel-note">Current/recent evidence-backed records</div></div></div><div id="overviewRecruiters" class="list"></div></section><section class="panel"><div class="panel-head"><div><h3>Safety</h3><div class="panel-note">Outbound actions remain disabled</div></div></div><div class="mini-grid"><div class="mini"><span>Live applications</span><strong>OFF</strong></div><div class="mini"><span>Emails sent</span><strong id="emailsSent">0</strong></div><div class="mini"><span>Recruiter outreach</span><strong>OFF</strong></div><div class="mini"><span>Gmail</span><strong>OFF</strong></div></div></section></div></div></div><div id="view-jobs" class="hidden"><section class="panel"><div class="panel-head"><div><h3>Job explorer</h3><div class="panel-note">Search company, role, technology, location or platform</div></div></div><div class="toolbar"><input id="jobSearch" placeholder="Search jobs…"><button class="filter active" data-decision="ALL">All</button><button class="filter" data-decision="APPLY">Apply</button><button class="filter" data-decision="REVIEW">Review</button><button class="filter" data-decision="REJECT">Reject</button></div><div id="jobsList" class="list"></div></section></div><div id="view-applications" class="hidden"><section class="panel"><div class="panel-head"><div><h3>Application queue</h3><div class="panel-note">Dry-run preparation only — nothing is submitted</div></div></div><div id="applicationsList" class="list"></div></section></div><div id="view-recruiters" class="hidden"><section class="panel"><div class="panel-head"><div><h3>Recruiter intelligence</h3><div class="panel-note">Identity, employer and hiring evidence stay explicit</div></div></div><div id="recruitersList" class="list"></div></section></div><div id="view-contacts" class="hidden"><section class="panel"><div class="panel-head"><div><h3>Public contact evidence</h3><div class="panel-note">An email found is not automatically a qualified hiring contact</div></div></div><div id="contactsList" class="list"></div></section></div><div id="view-content" class="hidden"><section class="panel"><div class="panel-head"><div><h3>Hiring content</h3><div class="panel-note">Public content with role and hiring evidence</div></div></div><div id="contentList" class="list"></div></section></div><div id="view-platforms" class="hidden"><section class="panel"><div class="panel-head"><div><h3>Platform coverage</h3><div class="panel-note">Catalog-only entries are shown but never counted as executed</div></div></div><div class="mini-grid" id="platformStats"></div><div class="toolbar" style="margin-top:14px"><input id="platformSearch" placeholder="Search platforms…"><button class="filter active" data-platform-filter="ALL">All</button><button class="filter" data-platform-filter="SUCCESS">Success</button><button class="filter" data-platform-filter="EMPTY">Empty</button><button class="filter" data-platform-filter="FAILED">Failed</button><button class="filter" data-platform-filter="TIMEOUT">Timeout</button><button class="filter" data-platform-filter="RATE_LIMITED">Rate limited</button><button class="filter" data-platform-filter="BLOCKED">Blocked</button><button class="filter" data-platform-filter="SKIPPED_CATALOG_ONLY">Catalog</button></div><div class="table-wrap"><table class="table"><thead><tr><th>Platform</th><th>Capability</th><th>Status</th><th>Last run</th><th>Fetched</th><th>Normalized</th><th>Inserted</th><th>Duplicates</th><th>Duration</th></tr></thead><tbody id="platformTable"></tbody></table></div></section></div><div id="view-sources" class="hidden"><section class="panel"><div class="panel-head"><div><h3>Source health</h3><div class="panel-note">Provider failures remain visible</div></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Source</th><th>Status</th><th>Fetched</th><th>Inserted</th><th>Duplicates</th><th>Failures</th><th>Last run</th></tr></thead><tbody id="sourceTable"></tbody></table></div></section></div><div class="footer-note">Job Agent · local product workspace · refreshed from live API</div></div><div id="detail" class="detail"><aside class="drawer"><div class="drawer-head"><div><div class="eyebrow">Details</div><h2 id="detailTitle" style="margin:4px 0 0;font-size:20px">—</h2></div><button class="close" id="closeDetail">×</button></div><div id="detailBody"></div></aside></div><script>
const state={summary:null,jobs:[],applications:[],recruiters:[],contacts:[],content:[],sources:[],platforms:null,decision:'ALL',platformFilter:'ALL'};const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const fmt=n=>Number(n||0).toLocaleString();const when=v=>v?new Date(v).toLocaleString([], {month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'}):'—';
function badgeDecision(d){const cls=d==='APPLY'?'apply':d==='REVIEW'?'review':'reject';return '<span class="decision '+cls+'">'+esc(d||'NO DECISION')+'</span>'}function empty(t,b){return '<div class="empty"><strong>'+esc(t)+'</strong><div style="margin-top:5px">'+esc(b)+'</div></div>'}function renderKpis(s){const items=[['Jobs',s.jobs,'Persisted opportunities'],['Matches',s.matches,'Latest decisions'],['APPLY',s.matchApply,'High-confidence queue'],['REVIEW',s.matchReview,'Needs review'],['REJECT',s.matchReject,'Filtered out'],['Ready',s.applications,'Prepared records'],['Recruiters',s.recruiters,'Current / recent'],['Contacts',s.contacts,'Qualified evidence'],['Content',s.content,'Hiring evidence']];$('kpis').innerHTML=items.map(x=>'<div class="kpi"><div class="label">'+esc(x[0])+'</div><div class="num">'+fmt(x[1])+'</div><div class="sub">'+esc(x[2])+'</div></div>').join('')}
function jobCard(x){return '<article class="job" data-job="'+esc(JSON.stringify(x))+'"><div class="job-title">'+esc(x.role||'Untitled role')+'</div><div class="job-company">'+esc(x.company||'Company unknown')+'</div><div class="job-meta">'+badgeDecision(x.decision||'REVIEW')+(x.score!=null?'<span class="badge score">Score '+esc(x.score)+'</span>':'')+(x.platform?'<span class="badge platform">'+esc(x.platform)+'</span>':'')+(x.workplaceType?'<span class="badge">'+esc(x.workplaceType)+'</span>':'')+'</div><div class="job-company" style="margin-top:8px">'+esc(x.location||'Location not specified')+'</div><div class="job-reason">'+esc(x.reason||'No decision explanation recorded.')+'</div>'+(x.url?'<a class="row-link" href="'+esc(x.url)+'" target="_blank" rel="noopener noreferrer">Open posting ↗</a>':'')+'</article>'}
function renderJobs(items,target){$(target).innerHTML=items.length?items.map(jobCard).join(''):empty('No jobs match this view','Try another decision filter or search term.')}
function renderApps(items,target){$(target).innerHTML=items.length?items.map(x=>'<article class="job"><div class="job-title">'+esc(x.role||'Application candidate')+' — '+esc(x.company||'Company pending')+'</div><div class="job-meta"><span class="badge safe">'+esc(x.status||'READY')+'</span><span class="badge">Dry run · not submitted</span>'+(x.score!=null?'<span class="badge score">Score '+esc(x.score)+'</span>':'')+'</div><div class="job-company" style="margin-top:8px">'+esc(x.location||'Location not specified')+'</div><div class="job-reason">'+esc(x.reason||'No match explanation recorded.')+'</div>'+(x.url?'<a class="row-link" href="'+esc(x.url)+'" target="_blank" rel="noopener noreferrer">Open job ↗</a>':'')+'</article>').join(''):empty('No application-ready records','Matching can prepare records, but live submission is disabled.')}
function renderRecruiters(items,target){$(target).innerHTML=items.length?items.map(x=>'<article class="job"><div class="job-title">'+esc(x.name||'Identity unavailable')+' · '+esc(x.company||'Company unknown')+'</div><div class="job-company">'+esc(x.role||'Role unavailable')+' · '+esc(x.relevance||'UNKNOWN')+'</div><div class="job-meta"><span class="badge">Confidence '+esc(x.confidence??'—')+'</span><span class="badge">Email '+esc(x.emailStatus||'UNKNOWN')+'</span><span class="badge">Mailbox evidence '+(x.mailboxEvidence?'yes':'no')+'</span>'+(x.eligibleForOutreach?'<span class="badge safe">OUTREACH ELIGIBLE</span>':'')+'</div>'+(x.profileUrl?'<a class="row-link" href="'+esc(x.profileUrl)+'" target="_blank" rel="noopener noreferrer">Evidence ↗</a>':'')+'</article>').join(''):empty('No qualified recruiter leads yet','Recruiter discovery runs independently. Only validated hiring evidence appears here.')}
function renderContacts(items,target){$(target).innerHTML=items.length?items.map(x=>'<article class="job"><div class="job-title">'+esc(x.email||'Email unavailable')+'</div><div class="job-company">'+esc(x.domain||'Domain unknown')+' · score '+esc(x.relevanceScore??'—')+' · '+esc(x.validationStatus||'UNKNOWN')+'</div><div class="job-reason">'+esc(x.evidenceContext||'No evidence context recorded.')+'</div>'+(x.sourceUrl?'<a class="row-link" href="'+esc(x.sourceUrl)+'" target="_blank" rel="noopener noreferrer">Source evidence ↗</a>':'')+'</article>').join(''):empty('No qualified public contacts yet','An email found is not promoted without evidence-backed qualification.')}
function renderContent(items,target){$(target).innerHTML=items.length?items.map(x=>'<article class="job"><div class="job-title">'+esc(x.role||'Hiring evidence')+' — '+esc(x.company||'Company pending')+'</div><div class="job-company">'+esc(x.sourceType||'source')+' · confidence '+esc(x.confidence??'—')+' · '+esc(when(x.observedAt))+'</div><div class="job-reason">'+esc(x.evidence||'Evidence context unavailable.')+'</div>'+(x.sourceUrl?'<a class="row-link" href="'+esc(x.sourceUrl)+'" target="_blank" rel="noopener noreferrer">Source ↗</a>':'')+'</article>').join(''):empty('No validated hiring content yet','Content discovery runs independently and only evidence-backed signals are shown.')}
function renderSources(items){$('sourceTable').innerHTML=items.map(x=>'<tr><td><strong>'+esc(x.name)+'</strong><div style="color:#98a2b3;margin-top:3px">'+esc(x.sourceType)+'</div></td><td>'+esc(x.lastRunStatus||'NOT RUN')+'</td><td>'+fmt(x.lastFetched)+'</td><td>'+fmt(x.lastInserted)+'</td><td>'+fmt(x.lastDuplicates)+'</td><td class="'+(Number(x.consecutiveFailures)?'bad':'ok')+'">'+fmt(x.consecutiveFailures)+'</td><td>'+when(x.lastFinishedAt||x.lastRunAt)+'</td></tr>').join('')}
function statusBadge(s){const cls=s==='SUCCESS_WITH_JOBS'?'safe':s==='SUCCESS_ZERO_JOBS'?'':'warn';return '<span class="badge '+cls+'">'+esc(s)+'</span>'}function renderPlatforms(){const p=state.platforms;if(!p)return;const vals=[['Registry',p.registry.total],['Executable',p.registry.executable],['Catalog only',p.registry.catalogOnly],['Attempted',p.latestRuntime.attempted],['Succeeded',p.latestRuntime.succeeded],['Empty',p.latestRuntime.empty],['Timeout',p.latestRuntime.timeout],['Rate limited',p.latestRuntime.rateLimited],['Blocked',p.latestRuntime.blocked],['Parser errors',p.latestRuntime.parserErrors],['Fetched',p.latestRuntime.fetched],['Normalized',p.latestRuntime.normalized],['Inserted',p.latestRuntime.inserted],['Duplicates',p.latestRuntime.duplicates]];$('platformStats').innerHTML=vals.map(x=>'<div class="mini"><span>'+esc(x[0])+'</span><strong>'+fmt(x[1])+'</strong></div>').join('');$('platformMini').innerHTML=[['Registry',p.registry.total],['Executable',p.registry.executable],['Attempted',p.latestRuntime.attempted],['Succeeded',p.latestRuntime.succeeded],['Fetched',p.latestRuntime.fetched],['Inserted',p.latestRuntime.inserted]].map(x=>'<div class="mini"><span>'+esc(x[0])+'</span><strong>'+fmt(x[1])+'</strong></div>').join('');$('platformProgress').style.width=(p.registry.executable?Math.min(100,p.latestRuntime.attempted/p.registry.executable*100):0)+'%';const q=($('platformSearch').value||'').toLowerCase();const f=state.platformFilter;const rows=p.platforms.filter(x=>(!q||x.platform_name.toLowerCase().includes(q)||x.platform_id.toLowerCase().includes(q))&&(f==='ALL'||x.outcome===f||(f==='FAILED'&&['ERROR','PARSER_ERROR'].includes(x.outcome))));$('platformTable').innerHTML=rows.map(x=>'<tr><td><strong>'+esc(x.platform_name)+'</strong><div style="color:#98a2b3;margin-top:3px">'+esc(x.platform_id)+'</div></td><td>'+esc(x.capability)+'</td><td>'+statusBadge(x.outcome)+'</td><td>'+when(x.completed_at)+'</td><td>'+fmt(x.fetched)+'</td><td>'+fmt(x.normalized)+'</td><td>'+fmt(x.inserted)+'</td><td>'+fmt(x.duplicates)+'</td><td>'+fmt(x.duration_ms)+' ms</td></tr>').join('')||'<tr><td colspan="9">No platform records for this filter.</td></tr>'}
async function getJson(path){const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw new Error(path+' '+r.status);return r.json()}async function refresh(){try{const [health,summary,apps,recs,contacts,content,sources,platforms]=await Promise.all([getJson('/healthz'),getJson('/api/summary'),getJson('/api/applications?limit=20'),getJson('/api/recruiters?limit=20'),getJson('/api/contacts?limit=20'),getJson('/api/content?limit=20'),getJson('/api/source-health'),getJson('/api/platform-coverage')]);state.summary=summary;state.applications=apps.applications||[];state.recruiters=recs.recruiters||[];state.contacts=contacts.contacts||[];state.content=content.content||[];state.sources=sources.sources||[];state.platforms=platforms;$('online').textContent='System online';$('apiStatus').textContent='Online';$('dbStatus').textContent=health.database==='ok'?'Healthy':'Degraded';$('lastRefresh').textContent=new Date().toLocaleTimeString();$('pending').textContent=fmt(summary.pendingTasks);$('emailsSent').textContent=fmt(summary.outreachSent);renderKpis(summary);renderJobs(summary.topMatches||[],'topMatches');renderApps(state.applications.slice(0,6),'overviewApps');renderRecruiters(state.recruiters.slice(0,5),'overviewRecruiters');renderApps(state.applications,'applicationsList');renderRecruiters(state.recruiters,'recruitersList');renderContacts(state.contacts,'contactsList');renderContent(state.content,'contentList');renderSources(state.sources);renderPlatforms()}catch(e){$('online').textContent='API unavailable';$('apiStatus').textContent='Unavailable';$('dbStatus').textContent='Check runtime'}}
async function loadJobs(){const q=encodeURIComponent($('jobSearch').value||'');const d=state.decision;try{const data=await getJson('/api/jobs?limit=60&search='+q+(d==='ALL'?'':'&decision='+d));state.jobs=data.jobs||[];renderJobs(state.jobs,'jobsList')}catch(e){$('jobsList').innerHTML=empty('Jobs unavailable','The live API did not return job records.')}}function showView(name){document.querySelectorAll('[id^="view-"]').forEach(x=>x.classList.add('hidden'));$('view-'+name).classList.remove('hidden');document.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x.dataset.view===name));if(name==='jobs')loadJobs()}function showDetail(x){$('detailTitle').textContent=x.role||x.platform_name||'Details';const pairs=[['Company',x.company],['Location',x.location],['Platform',x.platform],['Capability',x.capability],['Decision',x.decision],['Score',x.score],['Status',x.outcome],['Workplace',x.workplaceType],['Reason',x.reason],['Source URL',x.sourceUrl],['Posting URL',x.url],['Last run',x.completed_at],['Fetched',x.fetched],['Normalized',x.normalized],['Inserted',x.inserted],['Duplicates',x.duplicates],['Duration',x.duration_ms?x.duration_ms+' ms':null],['Error',x.error_detail]];$('detailBody').innerHTML=pairs.filter(p=>p[1]!=null&&p[1]!=='').map(p=>'<div class="detail-section"><h4>'+esc(p[0])+'</h4><div>'+esc(p[1])+'</div></div>').join('');$('detail').classList.add('open')}
$('closeDetail').onclick=()=>$('detail').classList.remove('open');$('detail').onclick=e=>{if(e.target===$('detail'))$('detail').classList.remove('open')};document.addEventListener('click',e=>{const card=e.target.closest('[data-job]');if(card)try{showDetail(JSON.parse(card.dataset.job))}catch(_){} });document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.view)));document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.go)));$('jobSearch').addEventListener('input',()=>loadJobs());document.querySelectorAll('[data-decision]').forEach(b=>b.addEventListener('click',()=>{state.decision=b.dataset.decision;document.querySelectorAll('[data-decision]').forEach(x=>x.classList.toggle('active',x===b));loadJobs()}));$('platformSearch').addEventListener('input',renderPlatforms);document.querySelectorAll('[data-platform-filter]').forEach(b=>b.addEventListener('click',()=>{state.platformFilter=b.dataset.platformFilter;document.querySelectorAll('[data-platform-filter]').forEach(x=>x.classList.toggle('active',x===b));renderPlatforms()}));refresh();setInterval(refresh,20000);
</script></body></html>`;
