import { PERMANENTLY_EXCLUDED_COMPANIES } from "../applications/ApplicationPolicy";
import { Database } from "../database/Database";
import { RecruiterContactCandidate, RecruiterDiscoveryContact, RecruiterDiscoveryInput, RecruiterDiscoveryProvider, RecruiterIdentityCandidate } from "./RecruiterDiscovery";
import { RecruiterDiscoveryRepository, StoredRecruiterContact } from "./RecruiterDiscoveryRepository";
import { RecruiterIdentityRepository, StoredRecruiterIdentity } from "./RecruiterIdentityRepository";
import { rankRecruiterContacts, RankedRecruiterContact } from "./RecruiterRanking";
export interface PersistentRecruiterDiscoveryOptions { provider: RecruiterDiscoveryProvider; repository: RecruiterDiscoveryRepository; identityRepository?: RecruiterIdentityRepository; cooldownHours?: number; minConfidence?: number; requireVerifiedEmail?: boolean; }
export interface RecruiterDiscoveryMetrics { recruiterDiscovery:{discovered:number;profileOnly:number;withEmail:number}; emailDiscovery:{attempted:number;found:number;notFound:number;invalid:number}; persistence:{insertedOrUpdated:number;deduplicated:number;rejected:number}; outreach:{eligible:number;prepared:number;sendQueued:number;sent:number;failed:number}; }
export interface PersistentRecruiterDiscoveryResult { status:"DISCOVERED"|"SKIPPED"; reason:string; runId:string|null; contacts:Array<StoredRecruiterContact & Pick<RankedRecruiterContact,"score"|"reasons">>; metrics:RecruiterDiscoveryMetrics; }
interface IdentityPersistence { upsertIdentity(companyName:string,companyDomain:string,candidate:RecruiterIdentityCandidate):Promise<StoredRecruiterIdentity>; addSources(contactId:string,candidate:RecruiterIdentityCandidate):Promise<void>; markEmailDiscovery(contactId:string,status:"FOUND"|"NOT_FOUND"|"INVALID"):Promise<void>; enrichEmail(contactId:string,email:string,verified:boolean,verificationStatus?:string,confidence?:number):Promise<StoredRecruiterIdentity|null>; }
function hasEmail(contact:RecruiterDiscoveryContact):contact is RecruiterContactCandidate{return typeof contact.email==="string"&&contact.email.trim().length>0;}
function normalize(value:string):string{return value.trim().toLowerCase();}
function normalizeDomain(value:string):string{return normalize(value).replace(/^https?:\/\//,"").split("/")[0]?.replace(/^www\./,"")??"";}
function isCompanyEmail(email:string,domain:string):boolean{return normalize(email).endsWith(`@${domain}`);}
function toStoredContact(identity:StoredRecruiterIdentity):StoredRecruiterContact|null{if(!identity.email)return null;return{id:identity.id,companyName:identity.companyName,companyDomain:identity.companyDomain,email:identity.email,fullName:identity.fullName,title:identity.title,department:identity.department,seniority:identity.seniority,country:identity.country,location:identity.location,confidence:identity.confidence,verified:identity.verified,verificationStatus:identity.verificationStatus,provider:identity.provider};}
class LegacyIdentityPersistenceAdapter implements IdentityPersistence{private readonly candidates=new Map<string,{companyName:string;companyDomain:string;candidate:RecruiterIdentityCandidate}>();constructor(private readonly repository:RecruiterDiscoveryRepository){}async upsertIdentity(companyName:string,companyDomain:string,candidate:RecruiterIdentityCandidate):Promise<StoredRecruiterIdentity>{if(!candidate.email)throw new Error("Legacy recruiter repository cannot persist profile-only identities; provide identityRepository.");const normalizedCandidate={...candidate,email:candidate.email.trim().toLowerCase()};const stored=await this.repository.upsertContact(companyName,companyDomain,normalizedCandidate);this.candidates.set(stored.id,{companyName,companyDomain,candidate:normalizedCandidate});return{...stored,email:stored.email,emailDiscoveryStatus:"FOUND",linkedinProfileUrl:candidate.linkedinProfileUrl};}async addSources(contactId:string,candidate:RecruiterIdentityCandidate):Promise<void>{await this.repository.addSources(contactId,{...candidate,email:candidate.email??""});}async markEmailDiscovery(_contactId:string,_status:"FOUND"|"NOT_FOUND"|"INVALID"):Promise<void>{}async enrichEmail(contactId:string,email:string,verified:boolean,verificationStatus?:string,confidence?:number):Promise<StoredRecruiterIdentity|null>{const existing=this.candidates.get(contactId);if(!existing)return null;const candidate={...existing.candidate,email:email.trim().toLowerCase(),verified,verificationStatus,confidence:confidence??existing.candidate.confidence};const stored=await this.repository.upsertContact(existing.companyName,existing.companyDomain,candidate);this.candidates.set(contactId,{...existing,candidate});return{...stored,email:stored.email,emailDiscoveryStatus:"FOUND",linkedinProfileUrl:candidate.linkedinProfileUrl};}}
export class PersistentRecruiterDiscoveryService{private readonly cooldownHours:number;private readonly minConfidence:number;private readonly requireVerifiedEmail:boolean;private readonly identityRepository:IdentityPersistence;constructor(private readonly options:PersistentRecruiterDiscoveryOptions){this.cooldownHours=options.cooldownHours??0;this.minConfidence=options.minConfidence??80;this.requireVerifiedEmail=options.requireVerifiedEmail??true;if(!Number.isFinite(this.cooldownHours)||this.cooldownHours<0)throw new Error("Recruiter discovery cooldown must be a non-negative finite number.");if(!Number.isFinite(this.minConfidence)||this.minConfidence<0||this.minConfidence>100)throw new Error("Recruiter discovery minimum confidence must be between 0 and 100.");if(options.identityRepository)this.identityRepository=options.identityRepository;else{const database=(options.repository as unknown as {database?:Database}).database;this.identityRepository=database?new RecruiterIdentityRepository(database):new LegacyIdentityPersistenceAdapter(options.repository);}}
async discoverAndPersist(input:RecruiterDiscoveryInput,maxContacts:number):Promise<PersistentRecruiterDiscoveryResult>{if(!input.candidateProfileId.trim())throw new Error("candidateProfileId is required for recruiter discovery.");if(!Number.isInteger(maxContacts)||maxContacts<1)throw new Error("maxContacts must be a positive integer.");const domain=normalizeDomain(input.companyDomain);if(!domain)throw new Error("A company domain is required for recruiter discovery.");if(isPermanentlyExcludedCompany(input.companyName))return{status:"SKIPPED",reason:"Company is permanently excluded from recruiter discovery and outreach.",runId:null,contacts:[],metrics:emptyMetrics()};if(this.cooldownHours>0&&await this.options.repository.hasRecentDiscovery(domain,this.options.provider.name,this.cooldownHours))return{status:"SKIPPED",reason:`A successful ${this.options.provider.name} discovery exists within the ${this.cooldownHours}-hour cooldown.`,runId:null,contacts:[],metrics:emptyMetrics()};const run=await this.options.repository.startDiscoveryRun({companyName:input.companyName,companyDomain:domain,jobOpportunityId:input.jobOpportunityId,candidateProfileId:input.candidateProfileId,provider:this.options.provider.name});const metrics=emptyMetrics();try{const discovered=await this.options.provider.discover(input);const identities=deduplicateRecruiterCandidates(discovered.contacts);metrics.recruiterDiscovery.discovered=identities.length;metrics.recruiterDiscovery.profileOnly=identities.filter((contact)=>!hasEmail(contact)).length;metrics.recruiterDiscovery.withEmail=identities.filter(hasEmail).length;const eligibleIdentities=identities.filter((contact)=>(contact.confidence??0)>=this.minConfidence);metrics.persistence.rejected+=identities.length-eligibleIdentities.length;const persisted=new Map<string,StoredRecruiterIdentity>();for(const candidate of eligibleIdentities){const stored=await this.identityRepository.upsertIdentity(input.companyName,domain,{...candidate,companyDomain:domain,provider:this.options.provider.name,email:candidate.email?.trim().toLowerCase()});await this.identityRepository.addSources(stored.id,candidate);persisted.set(stored.id,stored);metrics.persistence.insertedOrUpdated+=1;}const emailCandidates=await this.discoverEmails(input,discovered.contacts);metrics.emailDiscovery.attempted=[...persisted.values()].filter((contact)=>!contact.email||!contact.verified).length;const emailByIdentity=matchEmailCandidates([...persisted.values()],emailCandidates);for(const identity of persisted.values()){const candidate=emailByIdentity.get(identity.id)??(identity.email?{email:identity.email,fullName:identity.fullName,title:identity.title,confidence:identity.confidence,verified:identity.verified,verificationStatus:identity.verificationStatus,provider:identity.provider,sources:[]} as RecruiterContactCandidate:null);if(!candidate){if(!identity.email){await this.identityRepository.markEmailDiscovery(identity.id,"NOT_FOUND");metrics.emailDiscovery.notFound+=1;}continue;}if(!isCompanyEmail(candidate.email,domain)){await this.identityRepository.markEmailDiscovery(identity.id,"INVALID");metrics.emailDiscovery.invalid+=1;continue;}if(identity.email&&identity.verified)continue;const isJobPostingEvidence=this.options.provider.name==="job-posting"||(candidate.sources?.some((source)=>source.type==="job_posting")??false);let verified=candidate.verified;let verificationStatus=candidate.verificationStatus;let verificationConfidence=candidate.confidence;if(this.requireVerifiedEmail&&!verified&&!isJobPostingEvidence){try{const verification=await this.options.provider.verify(candidate.email);verified=verification.verified;verificationStatus=verification.status;verificationConfidence=Math.max(verificationConfidence??0,verification.confidence??0);}catch{verified=false;}}if(this.requireVerifiedEmail&&!verified&&!isJobPostingEvidence){await this.identityRepository.markEmailDiscovery(identity.id,"INVALID");metrics.emailDiscovery.invalid+=1;continue;}const enriched=await this.identityRepository.enrichEmail(identity.id,candidate.email,verified,verificationStatus,verificationConfidence);if(enriched){await this.identityRepository.addSources(enriched.id,candidate);persisted.set(identity.id,enriched);metrics.emailDiscovery.found+=1;}}
const outreachContacts:RecruiterContactCandidate[]=[...persisted.values()].map(toStoredContact).filter((contact):contact is StoredRecruiterContact=>Boolean(contact)).filter((contact)=>(contact.confidence??0)>=this.minConfidence).map((contact)=>({email:contact.email,fullName:contact.fullName,title:contact.title,department:contact.department,seniority:contact.seniority,country:contact.country,location:contact.location,confidence:contact.confidence,verified:contact.verified,verificationStatus:contact.verificationStatus,provider:contact.provider,sources:[]}));metrics.outreach.eligible=outreachContacts.length;const ranked=rankRecruiterContacts(outreachContacts,input.jobTitle,maxContacts);const resultContacts:Array<StoredRecruiterContact & Pick<RankedRecruiterContact,"score"|"reasons">>=[];for(const rankedContact of ranked){const stored=[...persisted.values()].find((identity)=>identity.email?.toLowerCase()===rankedContact.email.toLowerCase());const contact=stored?toStoredContact(stored):null;if(contact)resultContacts.push({...contact,score:rankedContact.score,reasons:rankedContact.reasons});}await this.options.repository.finishDiscoveryRun(run.id,"SUCCEEDED",persisted.size);const reason=`Discovered ${metrics.recruiterDiscovery.discovered}; profileOnly ${metrics.recruiterDiscovery.profileOnly}; withEmail ${metrics.recruiterDiscovery.withEmail}; emailFound ${metrics.emailDiscovery.found}; emailNotFound ${metrics.emailDiscovery.notFound}; invalid ${metrics.emailDiscovery.invalid}; outreachEligible ${metrics.outreach.eligible}; persisted ${metrics.persistence.insertedOrUpdated}.`;return{status:"DISCOVERED",reason,runId:run.id,contacts:resultContacts,metrics};}catch(error){const message=error instanceof Error?error.message:String(error);await this.options.repository.finishDiscoveryRun(run.id,"FAILED",0,message);throw error;}}
private async discoverEmails(input:RecruiterDiscoveryInput,discovered:RecruiterDiscoveryContact[]):Promise<RecruiterContactCandidate[]>{const direct=discovered.filter(hasEmail) as RecruiterContactCandidate[];const secondStage=this.options.provider.discoverEmails?await this.options.provider.discoverEmails(input):{provider:this.options.provider.name,contacts:direct,discoveredAt:new Date()};const staged=secondStage.contacts.filter(hasEmail) as RecruiterContactCandidate[];return deduplicateEmailCandidates([...direct,...staged]);}}
export function deduplicateRecruiterCandidates(contacts:RecruiterDiscoveryContact[]):RecruiterDiscoveryContact[]{const map=new Map<string,RecruiterDiscoveryContact>();for(const contact of contacts){const normalizedEmail=contact.email?.trim().toLowerCase();const key=normalizedEmail?`email:${normalizedEmail}`:contact.linkedinProfileUrl?.trim().toLowerCase()||`name:${normalize(contact.fullName??"")}|title:${normalize(contact.title??"")}`;const existing=map.get(key);if(!existing){map.set(key,{...contact,email:normalizedEmail??contact.email});continue;}const preferCandidate=hasEmail(contact)&&(!hasEmail(existing)||(!existing.verified&&contact.verified)||(existing.verified===contact.verified&&(contact.confidence??0)>(existing.confidence??0)));map.set(key,{...(preferCandidate?contact:existing),email:normalizedEmail??existing.email,fullName:existing.fullName??contact.fullName,title:existing.title??contact.title,department:existing.department??contact.department,seniority:existing.seniority??contact.seniority,confidence:Math.max(existing.confidence??0,contact.confidence??0),verified:existing.verified||contact.verified,linkedinProfileUrl:existing.linkedinProfileUrl??contact.linkedinProfileUrl,sources:[...existing.sources,...contact.sources]});}return[...map.values()];}
function deduplicateEmailCandidates(contacts:RecruiterContactCandidate[]):RecruiterContactCandidate[]{return deduplicateRecruiterCandidates(contacts).filter(hasEmail) as RecruiterContactCandidate[];}
function matchEmailCandidates(identities:StoredRecruiterIdentity[],candidates:RecruiterContactCandidate[]):Map<string,RecruiterContactCandidate>{const result=new Map<string,RecruiterContactCandidate>();for(const candidate of candidates){const match=identities.find((identity)=>{if(identity.linkedinProfileUrl&&candidate.linkedinProfileUrl)return identity.linkedinProfileUrl.toLowerCase()===candidate.linkedinProfileUrl.toLowerCase();if(identity.fullName&&candidate.fullName)return normalize(identity.fullName)===normalize(candidate.fullName);if(identity.email&&candidate.email)return identity.email.toLowerCase()===candidate.email.toLowerCase();return false;});if(match)result.set(match.id,candidate);}return result;}
function isPermanentlyExcludedCompany(companyName:string):boolean{const normalized=companyName.trim().toLowerCase();return PERMANENTLY_EXCLUDED_COMPANIES.some((company)=>company.toLowerCase()===normalized);}
function emptyMetrics():RecruiterDiscoveryMetrics{return{recruiterDiscovery:{discovered:0,profileOnly:0,withEmail:0},emailDiscovery:{attempted:0,found:0,notFound:0,invalid:0},persistence:{insertedOrUpdated:0,deduplicated:0,rejected:0},outreach:{eligible:0,prepared:0,sendQueued:0,sent:0,failed:0}};}
